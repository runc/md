{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "zeroConfig": true,
        "installCommand": "NPM_CONFIG_REGISTRY=https://registry.npmjs.org pnpm i --frozen-lockfile",
        "buildCommand": "pnpm web build:only",
        "outputDirectory": "apps/web/dist"
      }
    }
  ]
}
